name = "gemini-ai-studio-app"
compatibility_date = "2024-11-18"
compatibility_flags = ["nodejs_compat"]

# This line fixes the "invalid" warning in your logs
pages_build_output_dir = ".open-next/assets"

[vars]
# Your API key is handled in the Cloudflare Dashboard, but 
# you can reference it here if needed.
